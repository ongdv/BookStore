{
  "name": "CRUD",
  "version": "1.0.0",
  "main": "index.js",
  "author": "ongdv <ssokwoo@outlook.com>",
  "license": "MIT",
  "scripts": {
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "concurrently": "^4.0.1"
  }
}
